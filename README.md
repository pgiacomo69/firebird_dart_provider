Firebird Plugin for Dart/Flutter

## Features

This Package will provide connection to Firebird Rdbms for Dart. It is built with dart:ffi, so it will work where there is a firebird/interbase client library available.

